[pytest]
# E2E (Playwright) tests need a browser + running server, so they're excluded from
# the default run. CI's dedicated e2e job opts back in with `pytest -m e2e`.
addopts = -m "not e2e"
markers =
    e2e: end-to-end browser tests driving the real UI (requires pytest-playwright)
